[package]
name = "continuity-memory"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
sha2 = "0.10"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
